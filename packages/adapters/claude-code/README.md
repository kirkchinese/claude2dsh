# @claude2dsh/adapter-claude-code

Claude Code session-source adapter: discovery, parent-chain transcript parsing, skill discovery, and Claude Code JSONL serialization. Depends only on `@claude2dsh/core`.
